# りんご園の日記：更新方法

## はじめに

`blog-editor.html` をChromeまたはEdgeで開くと、日記を書く画面になります。トップページとメニューからは、読む人向けの `blog.html` を開けます。

今の構成は、記事を書いてファイルとして保存する方式です。**ブラウザの「下書きを保存」だけでは、ホームページには公開されません。** 園の方がログインして直接投稿するCMSは、公開サーバーが決まってから接続します。

## 1. 見本を読んでみる

1日分の見本「積み木あそびで見つけた、小さな『できた』」を入れています。

日付も出来事も架空の見本です。実際の活動としてそのまま公開せず、園での本当の出来事に書き換えてください。

文章は、次の4つの順で書くとまとめやすくなります。

1. 今日のあそび：どんな活動をしたか。
2. 小さな発見：表情、工夫、やってみたこと。
3. スタッフの関わり：どんな声かけや見守りをしたか。
4. 保護者へのひと言：今日の雰囲気が伝わる結び。

## 2. 記事を書く

1. 「＋ 新しい記事」を押します。既存記事は上の選択欄から選べます。
2. 日付・カテゴリ・タイトル・紹介文・本文を入力します。
3. イラストを選び、プレビューを確認します。
4. 実際の記録として掲載するときは「見本記事として表示する」を外します。
5. 作業を中断するときは「下書きを保存」を押します。

本文の空行が段落になります。「## 今日のあそび」のように書いた段落は見出しになります。HTMLの入力は必要ありません。

お子さまの実名など個人が特定される情報は載せず、写真を扱う場合は掲載許可を確認してください。今回は実際の活動写真の代わりにイラストを選べるようにしています。

## 3. サイトへ反映する

1. 内容を確認し「サイト用データを書き出す」を押します。
2. ダウンロードされた `blog-data.js` を保存します。
3. デスクトップの `apple-field` → `apple field` フォルダにある同名ファイルを、新しいファイルで置き換えます。置き換え前のファイルはバックアップしてください。
4. `blog.html` と `index.html` を再読み込みして確認します。必要なら強制再読み込みを行います。
5. 公開中のホームページにも反映する場合は、同じファイルを公開サーバーにアップロードします。

書き出しには現在の記事と、編集画面で読み込んでいる既存の記事が含まれます。同じ記事を編集した場合は、その記事を更新します。記事は日付の新しい順に表示されます。同日の記事は元の並びを保ちます。**未来の日付を指定しても予約投稿にはなりません。**

## 4. バックアップする

「バックアップ保存」で、記事データと現在の下書きをJSONファイルとして保存できます。「バックアップを読み込む」で復元できます。

下書き保存はこの端末・ブラウザ・開いている場所にひもづきます。端末やブラウザを変えた場合や、ブラウザのデータを消した場合には引き継がれません。別の人へ渡すときはバックアップを使ってください。

複数人の同時編集・ログイン管理・オンラインでの自動公開・予約投稿・写真アップロードは、この版にはありません。記事の削除が必要な場合は、データを管理する担当者へ依頼してください。公開サーバー決定後、必要な運用に合わせて追加します。

## 公開するとき

ブログも現在は検索非公開の確認用設定です。見本記事の扱いと、内容・写真の掲載確認後に公開します。編集用の `blog-editor.html`、`blog-editor.js`、バックアップJSONは作業用として手元に保管できます。公開サイトのメニューには編集画面のリンクを出していません。

一覧・記事ページはJavaScriptで表示します。検索エンジン向けの記事別メタ情報やSNS共有画像、直接投稿を重視する場合は、公開先に合わせて記事ごとのHTML生成またはCMSへ移行します。
